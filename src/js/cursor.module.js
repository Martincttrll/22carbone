// export const initCursorProjetHandler = () =>{
   
//         const cursor = document.querySelectorAll(".cursor-selector")[0];
//         const cursorHeight = getComputedStyle(cursor).height;
//         document.addEventListener('mousemove', (event) => {
//             let y = event.clientY;
//             cursor.style.top = y - (parseInt(cursorHeight) / 2) + "px";
//         })
    
// }